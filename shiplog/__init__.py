"""ShipLog — weekly retrospective digest for personal repos.

Reads merged PRs and notable commits over a time window across all
tracked repos, classifies them into conventional categories, and emits
a structured markdown digest plus a compact Discord message.
"""

__version__ = "0.1.0"
